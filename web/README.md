# Habbit Web

Web version of the Habbit habit tracker built with Next.js, React, TypeScript, and IndexedDB (Dexie).

## Getting started

```bash
cd web
npm install
npm run dev
```

Then open http://localhost:3000.
